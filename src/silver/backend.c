#include "renderer.h"

struct silver *silver_init(
        enum silver_backend id,
        struct hadal *hadal,
        const char *application_name)
{
    int32_t result;

    return NULL;
}

void silver_fini(struct silver *silver);
