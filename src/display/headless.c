#include "hadopelagic.h"

s32 hadal_headless_entry_point(
    struct hadopelagic *hadal, 
    struct allocation  *alloc,
    b32                 quiet)
{
    (void)hadal;
    (void)alloc;
    (void)quiet;
    return result_feature_unsupported;
}
