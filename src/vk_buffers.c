#include "vk_reznor.h"

FN_REZNOR_ASSEMBLY(vulkan, buffer) { (void)work; }
FN_REZNOR_DISASSEMBLY(vulkan, buffer) { (void)buffer; }

FN_REZNOR_ASSEMBLY(vulkan, bottom_level) { (void)work; }
FN_REZNOR_DISASSEMBLY(vulkan, bottom_level) { (void)bottom_level; }

FN_REZNOR_ASSEMBLY(vulkan, top_level) { (void)work; }
FN_REZNOR_DISASSEMBLY(vulkan, top_level) { (void)top_level; }
