#include "vk_reznor.h"

FN_REZNOR_ASSEMBLY(vulkan, descriptor_set_layout) { (void)work; }
FN_REZNOR_DISASSEMBLY(vulkan, descriptor_set_layout) { (void)descriptor_set_layout; }

FN_REZNOR_ASSEMBLY(vulkan, descriptor_set) { (void)work; }
FN_REZNOR_DISASSEMBLY(vulkan, descriptor_set) { (void)descriptor_set; }
